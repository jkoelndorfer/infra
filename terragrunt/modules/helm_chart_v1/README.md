# Terragrunt helm_chart Module

This module deploys Helm charts to a K3s cluster.

K3s ships with a custom resource that can manage entire Helm charts
or just elements of their configuration.

See https://docs.k3s.io/helm.
